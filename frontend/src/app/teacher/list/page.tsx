import { RoleListPage } from "@/components/role-list-page";

export default function TeacherListPage() {
  return (
    <RoleListPage
      role="teacher"
      title="Teachers"
      subtitle="Teacher directory with academic profile details from backend API."
    />
  );
}
